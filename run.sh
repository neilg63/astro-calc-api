# run this executable file from the installation directory to detect a custom .env 
# in the smae directory where the source is built via `cargo build --release`
# Can be used for pm2 to keep alive the server application/
target/release/astro_calc